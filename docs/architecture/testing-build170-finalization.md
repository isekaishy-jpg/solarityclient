# Testing Build 170: owned M2 finalization

Build 170 packages source `bc57f191ffa5feebee9a43a9f08cecf4ba73e015` from
`perf/critical-frame-work`. Installed through the Testing shortcut on
2026-09-20 at 16:44 EDT. Installed and packaged executable SHA-256:

`1EA4E67319F2426A5DEF6209288D0F3139ECBB1444451CD60377A26DDE2B866D`

Dirty identity reflects the reserved tracked `BUILD_NUMBER`. Source was committed
and the checkout clean before compilation. Tracked source and index remained
frozen throughout the numbered build.

## Change and validation

Completed geometry owners and contiguous renderer streams move together into
one shared CPU operation. It assembles packets/effects and orders transparency
using admitted compact scratch. Main consumes outputs at the original receiver
boundary; camera and stock callback/RNG ordering remain unchanged. Capacity
charges remain pinned in flight and retention. Folder modules separate admission,
ownership, streams and ordering. See the [contract and measurements](m2-owned-finalization.md).

- Formatting and workspace Clippy with all targets/features and warnings denied
  pass. All 1,642 tests pass, zero failed, 33 existing ignored, 101 suites.
- Four equipped comparisons complete 16,384 frames: matched stationary medians
  improve by 0.892-0.967 ms. Orbit medians are slightly worse and tails are mixed.
  An unarmed control pair completes 8,192 frames with a 0.145 ms regression.
- A separate equipped profile pair completes 8,192 frames. Main publication
  falls about 2.695 ms/frame, accompanied by increased coordinator waiting.
  That offload is not net frame savings. Main admission remains 3.853 ms and
  renderer CPU 4.572 ms in this fixture.
- Numbered optimized compilation and `-SkipBuild` installation pass. Revision,
  build number, hash and Testing shortcut are verified. Settings preserve four
  CPU workers, capacity 256, two network workers, 2560 x 1440 fullscreen-windowed
  and GPU 0. No interactive client was launched or interrupted.
- The [live Build 169 capture](live-build169-capture.md) overlapped numbered
  compilation. Controlled candidate measurements finished before packaging
  and did not overlap compilation.

Full cutover, main admission/renderer distribution, lighter-scene overhead,
gameplay hitches and the requested multi-ms total improvement remain open.
